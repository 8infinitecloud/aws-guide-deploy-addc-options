name: Infrastructure AD Deployment

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'plan'
        type: choice
        options:
        - plan
        - apply
        - destroy
      terraform_bucket:
        description: 'S3 bucket for Terraform state'
        required: true
        type: string
      aws_region:
        description: 'AWS region'
        required: true
        default: 'us-east-1'
        type: string
      skip_lock:
        description: 'Skip state locking (if DynamoDB table missing)'
        required: false
        default: false
        type: boolean

env:
  TF_VAR_aws_region: ${{ inputs.aws_region }}

jobs:
  terraform:
    name: Terraform ${{ inputs.action }}
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./infrastructure-ad
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.6.0
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ inputs.aws_region }}
    
    - name: Create backend configuration
      run: |
        cat > backend.tf << EOF
        terraform {
          backend "s3" {
            bucket         = "${{ inputs.terraform_bucket }}"
            key            = "infrastructure-ad/terraform.tfstate"
            region         = "${{ inputs.aws_region }}"
            encrypt        = true
            dynamodb_table = "terraform-state-lock"
          }
        }
        EOF
    
    - name: Terraform Init
      run: terraform init
    
    - name: Terraform Validate
      run: terraform validate
    
    - name: Terraform Plan
      if: inputs.action == 'plan'
      run: |
        if [ "${{ inputs.skip_lock }}" = "true" ]; then
          terraform plan -var-file="terraform.tfvars" -lock=false
        else
          terraform plan -var-file="terraform.tfvars"
        fi
    
    - name: Terraform Apply
      if: inputs.action == 'apply'
      run: |
        if [ "${{ inputs.skip_lock }}" = "true" ]; then
          terraform apply -var-file="terraform.tfvars" -auto-approve -lock=false
        else
          terraform apply -var-file="terraform.tfvars" -auto-approve
        fi
    
    - name: Terraform Destroy
      if: inputs.action == 'destroy'
      run: |
        if [ "${{ inputs.skip_lock }}" = "true" ]; then
          terraform destroy -var-file="terraform.tfvars" -auto-approve -lock=false
        else
          terraform destroy -var-file="terraform.tfvars" -auto-approve
        fi
